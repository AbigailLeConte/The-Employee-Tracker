-- did not need this here
-- DROP DATABASE IF EXISTS tracker_db;
-- CREATE DATABASE tracker_db;

-- USE tracker_db;

SELECT *
FROM department_db;

SELECT role_db,
FROM department_id
GROUP BY ?;

SELECT employee_db,
FROM role_id
GROUP BY Manager;
